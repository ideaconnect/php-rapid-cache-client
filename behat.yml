default:
    autoload:
        '': '%paths.base%/features/Contexts'
    suites:
        default:
            contexts:
                - FeatureContext
            paths:
                - '%paths.base%/features'
